"""Workflow implementations for LabPilot.

This directory contains AI-generated workflow implementations.
Workflows are created using the AI chatbot's workflow generation tools.

To create a new workflow:
1. Use the chatbot to describe your desired workflow
2. The AI will generate complete Python code
3. Review and modify the code as needed
4. Save it to this directory via the chatbot

Generated workflows will automatically appear in the manager list.
"""

from __future__ import annotations

__all__ = []

# Workflows are dynamically loaded from this directory
# Each workflow file should contain a workflow class that follows the standard pattern:
# - Config dataclass for parameters
# - Data dataclass for results
# - Main workflow class with initialize(), run(), cleanup(), and stop() methods

